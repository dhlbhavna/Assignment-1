#include <stdio.h>
#include <conio.h>

main(int argc, char *argv[])
{
       int i;

       for (i = 1; i < argc; i++)

           printf("Arg %d: %s\n",i, argv[i]);
            printf("\nThe number of arguments passed is  : %d",argc -1);
       getch();
       return 0;
}
